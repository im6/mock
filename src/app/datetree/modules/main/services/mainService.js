angular.module("app")
    .service('mainService',[
        "$rootScope",
        "utilityService",
        function($rootScope, utilityService){
            var privateFn = {
            };

            var output = {
            };

            return output;
    }]);
